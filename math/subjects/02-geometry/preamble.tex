% Standalone
\RequirePackage{standalone}

% === basics
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}

% === utils
\usepackage{blindtext} % To generate lorem ipsum
\usepackage{lipsum}  % produce dummy text as filler
\usepackage{color,soul} % highlight

% === AMS - American Mathematical Society
\usepackage{amsmath, booktabs}
\usepackage{amsthm}
\usepackage{amssymb}
\usepackage{amsfonts}
\usepackage{mathtools}

% must be place after AMS packages
\usepackage{MnSymbol} % Extra Symbols

% ==== Insert figures & tikz (Image-related packages)
\usepackage{graphicx}
\usepackage{subcaption}
% graphics path is relative to the master.tex file
\graphicspath{ {./figures/} {./figures/img/} }

%Path in Windows format:
%\graphicspath{ {c:/user/images/} }

% ============== Tikz ==============
% \usepackage{tikz}
% \usetikzlibrary{arrows.meta}

% === Page layout settings ===

% Set page margins
\usepackage[left=1.35in, right=1.35in, top=1.0in, bottom=.9in, headsep=.2in, footskip=0.35in]{geometry}

% Set line spacing
\renewcommand{\baselinestretch}{1.3}

% Set spacing between paragraphs
\setlength{\parskip}{1.3mm}

% === Dùng tiếng Việt
\usepackage[vietnamese=nohyphenation]{hyphsubst} % Để nó không hiện warning
\usepackage[vietnamese]{babel}

% === Hyperlink
\usepackage{hyperref} % must be the last package to import
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,      
    urlcolor=cyan,
    pdftitle={Overleaf Example},
    pdfpagemode=FullScreen,
}
    
\urlstyle{same}

% Multi-file LaTeX projects
\usepackage{subfiles} % Best loaded last in the preamble

% === Meta-data
\title{
	{Geometry notes}\\
	{\large tGoldenPhoenix}\\
	%{\includegraphics{university.jpg}}
}
\author{An Hào}
\date{07 March 2025}
