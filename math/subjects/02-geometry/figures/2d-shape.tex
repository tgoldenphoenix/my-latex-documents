% \documentclass[tikz, border=5pt]{standalone}
\documentclass[tikz]{standalone}

\usepackage{tikz}
\usetikzlibrary{arrows.meta}

% can specify RGB color
\usepackage{xcolor}

\begin{document}
  \begin{tikzpicture}
    \draw[lightgray] (0,0) grid (5,5);

    % \draw (1,1) circle [radius=0.1];
    % \fill[gray] (2,1) circle [radius=0.1];
    %
    % \draw (1,1) rectangle (2,2);

    \coordinate (A) at (3,3);

    % (Start angle : End angle : radius)
    % \draw (A) arc (30:90:2);
  \end{tikzpicture}
\end{document}
